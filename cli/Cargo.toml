[package]
name = "persian-tools-cli"
version = "1.1.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = {version = "4.5.4", features = ["derive"]}
rust-persian-tools = { path = "../" ,features = ['full']}
